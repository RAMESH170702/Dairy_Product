export type Product = {
    url?: string,
    name?: string,
    discountPrice?: string,
    actualPrice?: string,
    percentage?: string,
    category?: string,
    id?: number;
    quantity?: number;
}